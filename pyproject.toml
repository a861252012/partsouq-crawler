[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "partsouq-crawler"
version = "0.1.0"
description = "Evidence-first PartSouq crawler and NHTSA official-data sync"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [{ name = "Local workspace" }]
dependencies = [
    "aiohttp>=3.11,<4",
    "aiofiles>=24,<26",
    "aiosqlite>=0.20,<1",
    "certifi>=2025.1.31",
    "lxml>=5.3,<7",
    "pymysql[rsa]>=1.1,<2",
    "playwright>=1.55,<2",
    "warcio>=1.7,<2",
    "aiomysql>=0.3,<1",
    "flask>=3.1,<4",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.15,<2",
    "pytest>=8.3,<9",
    "pytest-cov>=6,<8",
    "ruff>=0.9,<1",
    "types-aiofiles>=24,<26",
    "types-PyMySQL>=1.1,<2",
]

[project.scripts]
partsouq-crawler = "partsouq_crawler.cli:main"
partsouq-admin = "partsouq_crawler.admin.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/partsouq_crawler"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["partsouq_crawler"]

[[tool.mypy.overrides]]
module = ["lxml.*", "warcio.*"]
ignore_missing_imports = true
